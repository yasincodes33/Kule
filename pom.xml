<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath />
	</parent>

	<groupId>com.yourcompany</groupId>
	<artifactId>agentsaas</artifactId>
	<version>0.1.0-SNAPSHOT</version>
	<name>agentsaas</name>
	<description>Agent SaaS Platform Backend</description>

	<properties>
		<java.version>21</java.version>
		<mapstruct.version>1.6.2</mapstruct.version>
	</properties>

	<dependencies>

		<!-- ===================== Web & Core ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-websocket</artifactId>
			<!-- Agent bridge bağlantısı + canlı log streaming için -->
		</dependency>

		<!-- ===================== Güvenlik ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-resource-server</artifactId>
			<!-- Kendi imzaladığımız JWT'leri doğrulamak için: NimbusJwtEncoder /
			NimbusJwtDecoder (bkz. config/JwtConfig.java). Boot 4'te JwtDecoder bean'i elle
			tanımlandığında OAuth2ResourceServerAutoConfiguration geri çekilir, issuer-uri
			gerekmez. -->
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-restclient</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
			<!-- Şifre sıfırlama ve organizasyon davet e-postaları JavaMailSender ile
			gönderiliyor, bkz. common/email. -->
		</dependency>
		<!-- ===================== Veri Erişimi ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
			<!-- Boot 4: FlywayAutoConfiguration artık spring-boot-flyway
			modülünde;
			migration'ların otomatik çalışması için ham flyway-core yerine starter
			gerekiyor -->
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
			<!-- Flyway 10+ Postgres desteği ayrı modül gerektiriyor -->
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aspectj</artifactId>
			<!-- Boot 4: spring-boot-starter-aop, aspectj starter'ı olarak
			yeniden adlandırıldı -->
		</dependency>

		<!-- ===================== Cache & Rate Limiting ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
			<!-- Cache + rate limiting (agent handshake, API throttling) -->
		</dependency>

		<!-- ===================== Async / Mesajlaşma ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
			<!-- TaskCreatedEvent, TaskCreatedEventKafkaBridge ile "task.created" topic'ine
			yayınlanır; TaskDispatchListener.onTaskCreatedFromKafka() bunu bir @KafkaListener
			olarak tüketir. -->
			<!-- Boot 4: KafkaAutoConfiguration artık spring-boot-kafka
			modülünde;
			ham spring-kafka yerine starter, otomatik yapılandırmayı (KafkaTemplate vb.)
			getirir -->
		</dependency>

		<!-- ===================== Gözlemlenebilirlik ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-micrometer-tracing-opentelemetry</artifactId>
			<!-- Boot 4: tracing otomatik yapılandırması
			(OpenTelemetryTracingAutoConfiguration)
			artık actuator-autoconfigure'da değil, bu ayrı modülde; olmadan tracing devreye
			girmez -->
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
			<!-- Distributed tracing: bir task'ın agent'a kadar izini sürebilmek
			için -->
		</dependency>

		<!-- ===================== API Dokümantasyonu ===================== -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.0.1</version>
		</dependency>

		<!-- ===================== Kod Üretimi / Boilerplate Azaltma
		===================== -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>${mapstruct.version}</version>
			<!-- Entity <-> DTO dönüşümleri için -->
		</dependency>

		<!-- ===================== Test ===================== -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${mapstruct.version}</version>
						</path>
						<!-- Lombok + MapStruct birlikte kullanılırken bu
						sıralama önemli -->
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok-mapstruct-binding</artifactId>
							<version>0.2.0</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>