# Kule web arayuzu — ortam degiskeni sablonu.
# Bu dosyayi `.env` olarak kopyalayin; `.env` surum kontrolune alinmaz.

# Backend REST API'sinin kok adresi.
VITE_API_BASE_URL=http://localhost:8081/api/v1

# WebSocket (canli gorev logu ve terminal) kok adresi.
VITE_WS_BASE_URL=ws://localhost:8081
